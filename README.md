# InnJoy Dashboards

This repository publishes the interactive InnJoy MPI dashboard as a static HTML site.

## Contents

- `index.html`: interactive cluster, movers, and action-queue dashboard

## Notes

- This repo intentionally excludes Python scripts and API keys.
- Regenerate `index.html` locally when data is refreshed, then update this repo.
